<resources xmlns:tools="http://schemas.android.com/tools">
    
    <!-- 1. Main Application Theme -->
    <!-- This is the theme used by your ProvisioningActivity -->
    <style name="Theme.Chimera" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- We keep this minimal as the UI is custom -->
    </style>

    <!-- 2. Transparent Theme for Invisible Camera Activity -->
    <!-- This is the theme that was causing the build to fail -->
    <style name="Theme.AppCompat.Transparent.NoActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    
</resources>
